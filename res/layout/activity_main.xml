<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="top"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/btClientes"
        android:layout_width="140dp"
        android:layout_height="100dp"
        android:layout_alignParentTop="true"
        android:layout_marginTop="25dp"
        android:drawableTop="@drawable/clientes"
        android:text="@string/clientes"
        android:textSize="@dimen/activity_main_buttons" />
    
    <Button
        android:id="@+id/btEquipamentos"
        android:layout_width="140dp"
        android:layout_height="100dp"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/btClientes"
        android:drawableTop="@drawable/equipamentos"
        android:text="@string/equipamentos"
        android:textSize="@dimen/activity_main_buttons" />

    <Button
        android:id="@+id/btOrcamentos"
        android:layout_width="140dp"
        android:layout_height="100dp"
        android:layout_alignLeft="@+id/btClientes"
        android:layout_below="@+id/btClientes"
        android:layout_marginTop="18dp"
        android:drawableTop="@drawable/orcamentos"
        android:text="@string/orcamentos"
        android:textSize="@dimen/activity_main_buttons" />

</RelativeLayout>